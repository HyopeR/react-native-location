import {StyleSheet} from 'react-native';

export const PageStyle = StyleSheet.create({
  root: {
    flex: 1,
    padding: 16,
    paddingTop: 8,
  },
  body: {
    flex: 1,
    marginBottom: 16,
  },
  description: {
    fontSize: 15,
    lineHeight: 20,
    textAlign: 'center',
    marginBottom: 16,
  },
});
